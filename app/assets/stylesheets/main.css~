/* General Page */

html {
  -webkit-background-size: cover;
  -moz-background-size: cover;
  -o-background-size: cover;
  background-size: cover;

  font-family: Trebuchet, sans;
  font-size: 62.5%;
  color: white;
}

html { margin: 0; padding: 0; }
body { margin: 105px 0 0 0; padding: 0; }

a, a:visited {
  text-decoration: none;
  color: white;
}

a.no_content {
  color: gray;
}

a:hover {
  color: DeepPink;
}

a.no_content:hover {
  color: #555;
}

a img { border: none; }

.hidden { display: none; }

#logo { display: block; height: 0; position: fixed; top: 0; left: 0; z-index: -1; }

.flash {
  border: solid gray 1px;
  padding: 10px;
  color: white;
  background: black;
  -webkit-border-radius: 6px;
  -moz-border-radius: 6px;
  border-radius: 6px;
}

#wrapper {
  height: 405px;
  width: 600px;
  margin: auto; 
}

#content {
  float: left;
  height: 365px;
  width: 560px;
  background: black;
}

nav {
  height: 50px;
  width: 100%;
  position: relative;
  top: 0px;
  left: 0px;
  font-size: 1.1em;
}

nav li {
  position: absolute;
  top: 18px;
}
nav #previous { left: 20px; }
nav #current { 
  width: 250px;
  position: absolute;
  left: 155px;
  text-align: center;
  font-weight: bold;
}
nav #next { right: 20px; }

/* Video List/Archive */
.post_list ul a:visited {
  color: #999;
}
.post_list_post {
  width: 140px;
  text-align: center;
  float: left;
  padding-top: 10px;
}

.post_list_post_container {
  width: 130px;
  margin: auto;
  padding-bottom: 8px;
  border: solid gray 1px;
}

ul {
  margin: 0;
  padding: 0;
}

li { list-style-type: none; }

/* Post View */

#video_list {
  width: 535px;
  margin: 80px auto;
  height: 142px;
}

#video_container {
  display: block;
  text-align: center;
  float: left;
  width: 172px;
  height: 140px;
  margin: 0 2px;
  border: solid gray 1px;
  -webkit-border-radius: 6px;
  -moz-border-radius: 6px;
  border-radius: 6px;
} 

#video_container img { padding-top: 8px; }

/* Add a Video */
input {
  width: 100px;
  background: #111;
  border: solid 1px MediumOrchid;
  color: white;
}
input[ type = "radio" ] {
  width: auto;
}

label { padding-left: 7px; }
input[name = "commit"]{ border: solid 1px white; background: #555; margin-left: 60%; }
input[name = "commit"]:hover { background: #444; }
input[name = "commit"]:active { background: #111; }

/* Attribution */
#attribution {
  z-index: -1;
  bottom: 10px;
  right: 10px;
  height: 90px;
  width: 140px;
  position: fixed;
  background: url('assets/attribution_bg.png');
  -webkit-border-radius: 6px;
  -moz-border-radius: 6px;
  border-radius: 6px;
}

#attribution_content {
  text-align: center;
  margin: auto;
  padding-top: 6px;
}

#attribution p {
  margin: 3px 0px;
}

.attr_name {
  font-size: 1.4em;
}


/******************
 * BORDERS        *
 * ***************/

.border {
  float: left;
}

.border_row {
}

/* (u)pper, (b)ottom, (m)iddle, (l)eft, (r)ight, (c)enter */
.border.u, .border.b { height: 20px; }
.border.m { height: 365px; }
.border.l, .border.r { width: 20px; }
.border.c { width: 560px; }

/* Some Small Screen Adjustments */

@media all and (max-height: 600px) { #wrapper { margin-top: 0; } }
@media all and (min-height: 600px) and (max-height: 700px) { #wrapper { margin-top: 10%; } }
@media all and (max-height: 800px) { #wrapper { margin-top: 13%; } }

